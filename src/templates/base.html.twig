<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Product Stock{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
              integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2"
              crossorigin="anonymous">
    {% endblock %}
</head>
<body>
<div class="container">

    <h1 class="text-center py-4 mb-0">Product Stock <img src="{{ asset('packages.png') }}"></h1>
    {% block body %}
        <h3 class="pb-3">Welcome to basic view!</h3>
        <div>List of endpoints:</div>
        <ul>
            <li class="font-weight-bold text-primary">view all products - /products</li>
            <li class="font-weight-bold text-primary">view products that are out of stock - /products/out</li>
            <li class="font-weight-bold text-primary">view products that are in stock - /products/in</li>
            <li class="font-weight-bold text-primary">view products that are low amount ( < or = 5 ) -
                /products/in/low
            </li>
            <li class="font-weight-bold text-primary">view products that are high amount ( > than 5 ) -
                /products/in/high
            </li>
            <li class="font-weight-bold text-warning">add new product - /products/new</li>
            <li class="font-weight-bold text-warning">edit existing product - /products/edit/{id}</li>
            <li class="font-weight-bold text-danger">delete existing product - /products/delete/{id}</li>
        </ul>
        <div>* {id} can be found in first column of table placed in /products view *</div>
        <div>To get some test data run sql commands from <span class="font-weight-bold">data-inserts.sql</span> or
            simply create couple new records via /products/new .
        </div>
    {% endblock %}
</div>
{% block javascripts %}{% endblock %}
</body>
</html>
